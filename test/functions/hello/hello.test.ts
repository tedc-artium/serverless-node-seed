// import { main } from "../../../src/functions/hello/handler"
// import { Context } from "aws-lambda";

describe('hello handler', () => {
    it('200: should return expected message', async () => {
        // const mockEvent = {
        //     body: {
        //         name: "fred"
        //     }
        // };

        // const expected = {
        //     statusCode: 200,
        //     body: JSON.stringify(`Hello ${mockEvent.body.name}, welcome to the exciting Serverless world!`)
        // };

        // const actual = await main(mockEvent, {} as Context);
        // expect(expected).toEqual(actual);
        expect(true).toBe(true);
    });
});